# PINNs-BasketOption-Extrapolation
Production-grade SciML framework &amp; benchmark suite solving multi-asset Black-Scholes PDEs via PINNs and hard-constraint ICPINNs. Proves structural superiority over pure NNs in OOD tail-risk domain extrapolation while achieving sub-millisecond inference vs. exact Monte Carlo, FDM, and Binomial tree baselines.
